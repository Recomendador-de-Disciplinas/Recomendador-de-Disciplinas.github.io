version: "3.7"
services:
  frontend:
    build: ./frontend
    ports:
      - 3000:8080
    volumes:
      - ./frontend:/usr/src/app

  backend:
    build: ./backend
    ports:
      - 8080:8080
    volumes:
      - ./backend:/usr/src/app

volumes:
  dbdata:
